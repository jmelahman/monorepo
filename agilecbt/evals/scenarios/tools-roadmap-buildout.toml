id = "tools-roadmap-buildout"
description = "Several writes in one turn: value, linked goal, and a first step."
tags = ["tools", "roadmap"]
kind = "roadmap"
time = "11:00"

[[turns]]
user = "ok let's do it: add a Health value, a goal 'Walk three times a week' under it, and a first step 'Walk around the block' for today"
[turns.expect]
tool_called = ["create_value", "create_goal", "create_step"]
[turns.expect.tool_args.create_goal]
value_id = 1
[turns.expect.tool_args.create_step]
lane = "today"
[turns.expect.db]
values = { min = 1, max = 1 }
goals = { min = 1, max = 1 }
