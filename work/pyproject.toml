# BEGIN orchard:manygo
# Shared by Go subtrees built into wheels with manygo:
# .config/git-orchard/shared/manygo in the monorepo
# (https://github.com/jmelahman/monorepo). Edit it there; `git orchard sync`
# copies it here.
[build-system]
# go-bin must cover the newest `go` directive among these subtrees' go.mod.
requires = ["hatchling", "hatch-vcs", "go-bin~=1.26", "manygo"]
build-backend = "hatchling.build"
# END orchard:manygo

[project]
name = "work-bin"
description = "A stupid simple time tracker"
license = {file = "LICENSE"}
authors = [{ name = "Jamison Lahman", email = "jamison@lahman.dev" }]
readme = "README.md"
requires-python = ">=3.6"
keywords = [
  "time-tracker", "time-management", "time",
]
classifiers = [
    "Programming Language :: Go",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dynamic = ["version"]


[project.urls]
Repository = "https://github.com/jmelahman/work"

[tool.hatch.build]
include = ["go.mod", "go.sum", "**/*.go"]

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.hooks.custom]
binary_name = "work"
